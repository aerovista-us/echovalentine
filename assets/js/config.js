// config.js
// Optional analytics (Umami):
// window.EV_CONFIG = { UMAMI_SRC: "https://umami.example/script.js", UMAMI_WEBSITE_ID: "xxxx-xxxx" };
window.EV_CONFIG = window.EV_CONFIG || {};

// Production base URL for share links. Must match exact deployed origin + path (e.g. GitHub Pages URL).
// If wrong, copied links will point to the wrong domain. Set before launch.
window.EV_CONFIG.BASE_URL = "https://aerovista-us.github.io/echovalentine";
